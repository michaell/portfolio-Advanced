extends ../template

block variables
  - var title = 'Добро пожаловать'

block content
  h1 Не главная
  form
    label(for="email") 
    input(name="email" id="email")
    label(for="password") 
    input(name="password" id="password")
    button Войти